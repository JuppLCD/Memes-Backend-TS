// ! Debo crear una util que vea que usuario quiere ver qe recurso, de lo contrario, si tengo roles voy a tener que cambiar la logica de los ModelUseCase

export default function () {
	// user.role === 'admin' || user.uuid === Meme.user_id
	if (true) {
	}
}
